<?php
/**
 * Маршруты модуля Search
 * 
 * Поиск — публичный функционал, доступен всем (включая гостей).
 * Middleware не требуются: GET-запросы не нуждаются в CSRF-защите.
 * 
 * @var \App\Core\Router $router
 */

use App\Modules\Search\Controllers\SearchController;

// =========================================================================
// ПУБЛИЧНЫЕ МАРШРУТЫ (доступны всем)
// =========================================================================

/**
 * Страница поиска.
 * Обрабатывает и вывод формы, и результаты через GET-параметры.
 * 
 * @example /search              → пустая форма поиска
 * @example /search?q=php        → результаты по запросу "php"
 * @example /search?q=php&page=2 → вторая страница результатов
 */
$router->add(
    'GET', 
    '/search', 
    SearchController::class . '@index', 
    'search.index'
);
