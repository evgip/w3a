<?php

/**
 * Конфигурация модуля Content
 * 
 * Настройки парсинга Markdown и обработки текста.
 */

return [
    // ═══════════════════════════════════════════
    // 🔑 MARKDOWN (настройки парсера Parsedown)
    // ═══════════════════════════════════════════
    
    'markdown' => [
        // Включить SafeMode (защита от XSS - экранирует HTML-теги в тексте)
        // ⚠️ Рекомендуется держать включённым для безопасности
        'safe_mode' => true,
        
        // Экранировать HTML-разметку внутри Markdown
        'markup_escaped' => true,
        
        // Автоматически превращать URL-адреса в ссылки
        'urls_linked' => true,
    ],

    // ═══════════════════════════════════════════
    // 🗂️ КЭШИРОВАНИЕ
    // ═══════════════════════════════════════════
    
    'cache' => [
        // Включить кэширование результатов парсинга
        // Ускоряет работу, но требует очистки при изменении контента
        'enabled' => env('CONTENT_CACHE_ENABLED', true),
        
        // Время жизни кэша в секундах (по умолчанию 1 час)
        'ttl' => 3600,
        
        // Путь к кэшу относительно storage/
        'path' => 'cache',
    ],

    // ═══════════════════════════════════════════
    // 🔗 ССЫЛКИ (обработка <a href="...">)
    // ═══════════════════════════════════════════
    
    'links' => [
        // Добавлять target="_blank" для внешних ссылок
        // Открывает ссылки в новой вкладке
        'external_blank' => true,
        
        // Добавлять rel="noopener noreferrer" для безопасности
        // Защищает от уязвимостей window.opener
        'external_secure' => true,
        
        // Внутренние домены (не считаются внешними)
        // Ссылки на эти домены не получают target="_blank"
        'internal_domains' => [
            env('APP_URL', 'http://localhost'),
        ],
    ],

    // ═══════════════════════════════════════════
    // 👤 @УПОМИНАНИЯ (парсинг @username)
    // ═══════════════════════════════════════════
    
    'mentions' => [
        // Включить обработку @упоминаний в тексте
        // Пример: @username превращается в ссылку на профиль
        'enabled' => true,
        
        // Минимальная длина имени пользователя
        'min_length' => 3,
        
        // Максимальная длина имени пользователя
        'max_length' => 20,
        
        // Шаблон URL профиля пользователя
        // {username} будет заменён на реальное имя
        'profile_url' => '/user/{username}',
    ],

    // ═══════════════════════════════════════════
    // 🖼️ ИЗОБРАЖЕНИЯ
    // ═══════════════════════════════════════════
    
    'images' => [
        // Разрешить изображения в постах/историях
        'allowed_in_posts' => true,
        
        // Разрешить изображения в комментариях
        // ⚠️ Обычно отключают для защиты от спама
        'allowed_in_comments' => false,
    ],

    // ═══════════════════════════════════════════
    // 🚫 ЧЁРНЫЙ СПИСОК (фильтрация доменов)
    // ═══════════════════════════════════════════
    
    'blacklist' => [
        // Запрещённые домены в ссылках
        // Ссылки на эти домены будут заменены на [ссылка заблокирована]
        'domains' => [
            // 'spam-site.com',
            // 'malware.ru',
        ],
    ],
];