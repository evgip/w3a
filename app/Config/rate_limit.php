<?php

/**
 * Настройки ограничения запросов (Rate Limiting)
 */

return [
    // Глобальное включение/отключение
    'enabled' => true,
    
    // Вероятность сборки мусора (1-100%)
    'gc_probability' => 5,
    
    // Время ожидания после блокировки (секунды)
    'retry_after' => 60,
    
    // Правила для разных действий
    'rules' => [
        // Пример правила для регистрации
        'register' => [
            'max_requests' => 5,      // Максимум запросов
            'window' => 3600,         // Временное окно (1 час)
        ],
        
        // Пример правила для входа
        'login' => [
            'max_requests' => 10,
            'window' => 600,          // 10 минут
        ],
        
        // Пример правила для отправки комментариев
        'comment' => [
            'max_requests' => 30,
            'window' => 60,           // 1 минута
        ],
    ],
];